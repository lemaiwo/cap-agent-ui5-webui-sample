node_modules/
gen/
*.log

# Credentials written by `cds bind` and CAP tooling. These hold live service
# keys for real BTP instances - never commit them.
.cdsrc-private.json
default-env.json
*.pem

# MTA build output
mta_archives/
.mta/
test-results/
playwright-report/

# Saved browser session from `npm run login`. Contains live auth cookies for a
# deployed app - as sensitive as a token. Never commit.
.auth/

# Generated by `npm run build:ui` from the installed plugin. Build output, not
# source - regenerate it, never edit it.
app/chat-ui/

# mbt staging folder for html5 content
resources/
